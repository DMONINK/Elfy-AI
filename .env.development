GOOGLE_AI_KEY=
DISCORD_BOT_TOKEN=
OWNER_IDS=
# Dashboard login password — required for the web dashboard (web_dashboard.py)
# to serve anything. Pick any password and set it here (and in Replit
# Secrets for the deployed bot).
DASHBOARD_PASSWORD=
